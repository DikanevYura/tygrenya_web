import type { Viewport } from 'next';

export const viewportThemeColor = {
  home: '#eff4fb',
  statistic: '#fbfcfd',
  about: '#eff3fa',
  contacts: '#eff4fb',
  post: '#fbfcfd',
};

const viewport: Viewport = {
  themeColor: '#fbfcfd',
};

export { viewport };
